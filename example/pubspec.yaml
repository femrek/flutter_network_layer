name: example
description: "An example project to use the flutter layer manager"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # flutter_network_layer Packages
  flutter_network_layer_dio:
    path: ../flutter_network_layer_dio

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
